version: '3.6'

services:
  alpine-nginx-php-amd64:
    build: .
    image: hackinglab/alpine-nginx-php-amd64:3.2
    environment:
    - AUTHOR=e1
    - HL_USER_USERNAME=hacker
    ports:
      - 80:80


